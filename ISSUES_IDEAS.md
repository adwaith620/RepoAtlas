# Good First Issues
1. Support more languages (Java, Go, Rust) in `parseImports`.
2. Add a GitHub App integration for private repos.
3. Add a VS Code extension wrapping the frontend.
4. Visualize folder groupings physically in React Flow using `parentNode`.
5. Support local directories instead of just GitHub URLs.
